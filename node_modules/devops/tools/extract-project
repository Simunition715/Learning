#!/bin/sh
# Run this script from the project root. This assumes you have a ./project.tgz
# file at the root of the project to be extracted. This is intended to be a very
# fast overwrite of the project with the contents of another project. So use
# cautiously.
tar -zxvf ./project.tgz -C ./
