/**
 * This will build and run the static storybook files. This is mostly for
 * testing and one should consider using the storybook heroku deployment
 * strategy as heroku nodes have a hard time building the storybook static
 * files due to lack of RAM.
 */
export default async function run() {
  // TODO
  console.warn("NOT IMPLEMENTED YET");
}
