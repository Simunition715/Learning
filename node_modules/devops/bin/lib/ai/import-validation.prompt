// ${{filePath}} - the file to validate imports
${{fileContent}}

Rewrite the code so that the import statements in this code block are relative to the current file given the project file structure above.

- `${{pascalName}}` is in ${{componentPath}}
- `${{pascalName}}Props` is in ${{componentPropsPath}}
- `${{pascalName}}Mode` is in ${{componentTypesPath}}
- `I${{pascalName}}` is in ${{componentTypesPath}}

Delete any imports that are unused or unnecessary.