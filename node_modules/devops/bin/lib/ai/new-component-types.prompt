Using the following sample-component.types.ts file as inspiration, please add props to the I${{componentName}} props interface in a ${{fileName}}.types.ts file based on the component definition within ${{fileName}}.tsx

// sample-component.types.ts
Don't delete any of these exports
/** Rendering modes of this component */
export const ${{componentName}}Mode = {} as const;

export type ${{componentName}}ModeType =
  typeof ${{componentName}}Mode[keyof typeof ${{componentName}}Mode];

/**
 * ${{componentName}} props
 */
export interface I${{componentName}} {
  /** Provides a custom class name to the container of this component */
  className?: string;
  /** Props to apply directly to the container div of this component */
  containerProps?: React.HTMLProps<HTMLDivElement>;
  /** Rendering mode of this component */
  mode?: ${{componentName}}ModeType;
}