import { stripIndent } from "common-tags";

/**
 * Tagged template literal that formats a string to be standardized and
 * appropriate for commander descriptions.
 */
export function description(str: TemplateStringsArray, ...args: any[]) {
  return `${stripIndent(str, ...args)}\n\n`;
}
