import fs from "fs-extra";
import path from "path";

export async function targetProjectRepo() {
  const packageJson = fs.readJSONSync(path.resolve("package.json"));
  return packageJson.repository || {};
}
