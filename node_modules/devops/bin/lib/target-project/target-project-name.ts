import fs from "fs-extra";
import path from "path";

export async function targetProjectName() {
  const packageJson = fs.readJSONSync(path.resolve("package.json"));
  return packageJson.name || "";
}
